{#
/**
 * @component Wudo Carousel
 *
 * A flexible, accessible carousel component implemented as a Web Component.
 * Designed to display a collection of items with optional looping and
 * configurable items per view.
 *
 * - Uses CSS custom properties for layout configuration.
 * - JavaScript enhances behavior (navigation, dots, keyboard interaction).
 * - Follows WAI-ARIA carousel best practices.
 *
 * Available variables:
 * - items (rendered content): Carousel items (slides).
 * - items_per_view (int, optional): Number of items visible at once (default: 1).
 * - loop (bool, optional): Whether the carousel should loop infinitely.
 *
 * Accessibility:
 * - Uses role="region" with aria-roledescription="carousel".
 * - Navigation buttons include accessible labels.
 * - Pagination dots are rendered dynamically with role="tablist".
 *
 * Note:
 * - The dots inside `.carousel__dots` are generated by JavaScript
 *   based on the number of child elements provided in the items slot.
 */
#}

<wudo-carousel
  class="carousel"
  style="--dc-items: {{ items_per_view|default(1) }};"
  data-items="{{ items_per_view|default(1) }}"
  data-loop="{{ loop ? 'true' : 'false' }}"
  role="region"
  aria-roledescription="carousel"
>
  <div class="carousel__viewport">
    <div class="carousel__track">
      {{ items }}
    </div>
  </div>

  <div class="carousel__controls">
    <button type="button" class="carousel__btn carousel__btn--prev" aria-label="Previous slide">
      {{ include('wudo:icon', { symbol: 'left-arrow' }, with_context = false) }}
    </button>

    <div class="carousel__dots" role="tablist">
      {# JS generated content #}
    </div>

    <button type="button" class="carousel__btn carousel__btn--next" aria-label="Next slide">
      {{ include('wudo:icon', { symbol: 'right-arrow' }, with_context = false) }}
    </button>
  </div>
</wudo-carousel>
